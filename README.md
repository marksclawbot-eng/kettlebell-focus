# Kettlebell Focus PWA

Static offline workout timer for Mark's kettlebell sessions.

## Features

- Runs from GitHub Pages with no backend.
- Installable PWA; service worker caches the app for offline use.
- Generates one of Mark's current kettlebell programs, avoiding recent local choices.
- Focus mode with 45s work blocks, 15s transitions, and 60s round rests.
- Attempts to keep the screen awake via the Wake Lock API.
- Optional voice guidance using browser text-to-speech.
- Records tap/timer timings and produces a copyable workout summary.
- Pause freezes the exercise countdown while elapsed session time keeps running, so summaries include pauses.

## Deployment

This folder is intended to be served by GitHub Pages from `docs/`.
The app path is `/kettlebell/` relative to the Pages site root.

No secrets, accounts, or backend calls are used.

## 2026-05-18 update

- Work intervals stop at 0 and wait for a manual Next/Start rest click before the rest/transition timer begins.
- Rest and transition intervals auto-advance to the next exercise.
- Focus mode displays local cropped exercise images from `assets/`.

## 2026-06-25 update

- Replaced the app program list with the six approved balanced sessions.
- Preserved Mark’s programming preferences: approved exercise list only, left/right blocks back-to-back, no wall sits or goblet squat holds, snatches in two sessions, and reduced pullovers/overrepresented filler.

## 2026-06-25 prep/layout update

- Added a 5-second get-ready countdown before the first work block.
- Tightened focus-mode layout for iPhone browser chrome: header hides during workouts, padding/timer/image/buttons are more compact, and the workout screen avoids vertical scrolling.

## 2026-06-25 heat map update

- Added app links to the generated session heat map.
- Heat map files are regenerated by `skills/kettlebell-coach/scripts/kb.py log ...` and can be refreshed manually with `refresh-heatmap`.
